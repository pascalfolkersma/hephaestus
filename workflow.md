# Agent workflow — Hephaestus

This diagram shows the rendered agent set and their typical dispatch relationships.
Node fill colors come from each agent's `color:` frontmatter (ADR 0006 palette) so multi-agent runs
are visually distinguishable. Executors are rectangular; planners are stadium-shaped; the orchestrator
is the dispatch hub.

```mermaid
flowchart TD
  classDef c_red fill:#7a2d2d,stroke:#f87171,color:#fff
  classDef c_blue fill:#2d4a7a,stroke:#60a5fa,color:#fff
  classDef c_green fill:#2d7a3a,stroke:#4ade80,color:#fff
  classDef c_cyan fill:#2d6a7a,stroke:#22d3ee,color:#fff
  classDef c_orange fill:#e8922a,stroke:#fb923c,color:#fff
  classDef c_pink fill:#7a2d5b,stroke:#f472b6,color:#fff
  classDef c_teal fill:#2d7a6a,stroke:#2dd4bf,color:#fff
  classDef c_purple fill:#5b2d7a,stroke:#c084fc,color:#fff

  %% Orchestrator
  orchestrator["orchestrator"]

  %% Executors
  bug_fixer["bug-fixer"]
  developer["developer"]
  git_commit_push["git-commit-push"]
  idea_architect["idea-architect"]
  test_writer["test-writer"]

  %% Planners
  reviewer(["reviewer"])
  sync_check(["sync-check"])

  %% Orchestrator dispatches to executors
  orchestrator -->|dispatch| bug_fixer
  orchestrator -->|dispatch| developer
  orchestrator -->|dispatch| git_commit_push
  orchestrator -->|dispatch| idea_architect
  orchestrator -->|dispatch| test_writer

  %% Executors hand off to git-commit-push
  bug_fixer -->|done| git_commit_push
  developer -->|done| git_commit_push
  idea_architect -->|done| git_commit_push
  test_writer -->|done| git_commit_push

  %% Planners are side-channel consultants
  orchestrator -. consults .-> reviewer
  orchestrator -. consults .-> sync_check

  class bug_fixer c_red
  class developer c_blue
  class git_commit_push c_green
  class idea_architect c_cyan
  class orchestrator c_orange
  class reviewer c_pink
  class sync_check c_teal
  class test_writer c_purple
```

> Generated by Hephaestus. Re-run `hephaestus init` to update after changing the agent set.
